-- A coluna gallery já existe como jsonb, mas vamos garantir que esteja pronta
-- O código já usa 'gallery' para imagens e vídeos.
-- Se houver necessidade de migração de campos antigos, faríamos aqui.
-- Como o projeto já parece usar 'gallery', apenas confirmamos a estrutura.
SELECT 1; 
